import favorite from "../resources/icons/favorite.svg";

export const getIcon = (iconName) => {
  switch (iconName) {
    case "favorite":
      favorite;
      break;
    case "favorite":
      favorite;
      break;
    default:
      favorite;
  }
};
